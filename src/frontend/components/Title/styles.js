import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        padding: 10,
        display: 'flex',
        flexDirection: 'column',
        marginTop: 12,
    },

    title: {
        fontSize: 24,
    },

    subtitle: {
        fontSize: 16,
        color: 'grey'
    },
    
});